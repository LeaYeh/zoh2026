run_name: chronos_zeroshot_electricity

dataset:
  name: electricity

model:
  name: chronos
  checkpoint: amazon/chronos-t5-small
  context_length: 512
  prediction_length: 24
  num_samples: 20
  finetune:
    method: none

training:
  max_steps: 0
  batch_size: 8
  learning_rate: 1.0e-4
