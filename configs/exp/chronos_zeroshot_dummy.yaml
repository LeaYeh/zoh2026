run_name: chronos_zeroshot_dummy

dataset:
  name: dummy
  n_series: 20
  length: 256
  prediction_length: 24

model:
  name: chronos
  checkpoint: amazon/chronos-t5-small
  context_length: 128
  prediction_length: 24
  num_samples: 20
  finetune:
    method: none

training:
  max_steps: 0
  batch_size: 8
  learning_rate: 1.0e-4
